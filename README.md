# Monotonic FastAPI Clock

## Install

```powershell
python -m pip install -r requirements.txt
```

## Run

From this folder:

```powershell
python -m uvicorn app:app --reload
```

Then open:

http://127.0.0.1:8000

Optional API test:

http://127.0.0.1:8000/api/time
